{
  "name": "sarm-cre-workflows",
  "version": "1.0.0",
  "description": "Chainlink Runtime Environment workflows for SARM Protocol SSA rating updates",
  "type": "module",
  "scripts": {
    "build": "cre compile workflows/ssa-refresh.ts",
    "dev": "cre simulate workflows/ssa-refresh.ts",
    "deploy": "cre deploy workflows/ssa-refresh.ts"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "latest"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
